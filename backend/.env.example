PORT=3001
JWT_SECRET=change_me_in_production
FRONTEND_URL=http://localhost:5173

# PostgreSQL (Docker local)
DATABASE_URL=postgresql://vriends:vriends@127.0.0.1:5435/vriends

# En production Railway : utiliser la DATABASE_URL fournie par le plugin Postgres
# Ne plus utiliser DB_PATH (SQLite)
